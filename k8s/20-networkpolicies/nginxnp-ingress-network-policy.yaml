apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nginxnp
  namespace: myns
spec:
  podSelector:
    matchLabels:
      run: nginxpod
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          run: curlpod
