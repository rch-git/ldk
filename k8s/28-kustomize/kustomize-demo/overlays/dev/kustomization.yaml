apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# this is appended to finance- from the previous template - /home/sysuser/git/ldk/k8s/28-kustomize/kustomize-demo/base/kustomization.yaml
namePrefix: dev-
nameSuffix: -v1

labels:
  - includeSelectors: true
    pairs:
      env: dev