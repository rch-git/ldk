apiVersion: v1
kind: Service
metadata:
  name: {{ include "flappydock.fullname" . }}
  labels:
    {{- include "flappydock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flappydock.selectorLabels" . | nindent 4 }}
